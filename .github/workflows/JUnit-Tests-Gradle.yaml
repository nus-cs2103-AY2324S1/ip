name: JUnit Tests with Gradle
run-name: run JUnit Tests for Rat
on:
  push:
    branches:
      - master
jobs:
  Build-Gradle:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Java
        uses: actions/setup-java@v2
        with:
          distribution: 'adopt'
          java-version: '11'

      - name: Run ./gradlew test
        run: |
          chmod +x ./gradlew
          ./gradlew test

      - name: Run Checkstyle
        run: |
          chmod +x ./gradlew
          ./gradlew checkstyleMain checkstyleTest
