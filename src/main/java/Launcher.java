import javafx.application.Application;

/**
 * Launcher is the class that will provide the entry point to our application.
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(VedaGui.class, args);
    }
}
