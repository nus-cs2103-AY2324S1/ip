package commands;

import duke.TaskListStorage;

public class TodoCommand implements Command {
    @Override
    public void execute(TaskListStorage taskListStorage) {
        // Actual logic for handling the "todo" command
    }
}