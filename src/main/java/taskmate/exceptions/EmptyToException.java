package taskmate.exceptions;

/**
 * The EmptyToException class is a child class of the Exception class. It is thrown when the user formats their
 * `event` command incorrectly, leaving the "to" clause of the `event` command empty.
 */
public class EmptyToException extends Exception {
}
