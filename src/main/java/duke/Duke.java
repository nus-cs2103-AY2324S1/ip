package duke;

import java.util.List;
import java.util.Scanner;
import javafx.application.Application;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.control.ScrollPane;
import javafx.scene.control.TextField;
import javafx.scene.layout.AnchorPane;
import javafx.scene.layout.VBox;
import javafx.stage.Stage;
import javafx.scene.layout.Region;
import javafx.scene.control.Label;
import javafx.scene.image.Image;
import javafx.scene.image.ImageView;
import javafx.collections.FXCollections;
import javafx.collections.ObservableList;
import javafx.geometry.Pos;
import javafx.scene.Node;
import javafx.scene.control.Label;
import javafx.scene.image.ImageView;
import javafx.scene.layout.HBox;


/**
 * The Duke class represents the core of the Duke chatbot application.
 * It manages the chatbot's functionality and responses.
 */
public class Duke {

    /**
     * The TaskList that stores the tasks in the application.
     */
    public static TaskList taskList = new TaskList();

    /**
     * Constructor for the Duke class.
     */
    public Duke() {
        // ...
    }


    /**
     * Gets a response from the chatbot based on the user's input.
     *
     * @param input The user's input message.
     * @return A response generated by the chatbot.
     */
    public String getResponse(String input) {
            return Parser.parse(input);
    }
}



