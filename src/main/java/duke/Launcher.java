package duke;

import javafx.application.Application;

/**
 * A launcher class to start the GUI.
 */
public class Launcher {
    /**
     * Launches the GUI.
     *
     * @param args arguments.
     */
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
