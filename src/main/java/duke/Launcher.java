package duke;

import javafx.application.Application;

/**
 * Launcher class that we run through IntelliJ to launch the bot.
 */
public class Launcher {
    public static void main(String[] args) {
        Application.launch(Main.class, args);
    }
}
