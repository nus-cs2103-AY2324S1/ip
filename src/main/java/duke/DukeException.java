package duke;

/**
 * An exception class that handles and parses the type of error.
 */
public class DukeException extends Exception{
    public DukeException(String message){
        super(message);
    }
}
