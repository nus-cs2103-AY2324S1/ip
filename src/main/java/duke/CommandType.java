package duke;

/**
 * Represents the command types that the chatbot can recognise.
 */
public enum CommandType {
    LIST, MARK, UNMARK, BYE, TODO, DEADLINE, EVENT, DELETE, DEADLINEBY, EVENTFROM, EVENTTO;
}
