package duke;

/**
 * Represents the command types that the chatbot can recognise.
 */
public enum CommandType {
    LIST, MARK, UNMARK, TODO, DEADLINE, EVENT, DELETE, DEADLINEBY, EVENTFROM, EVENTTO, FIND;
}
