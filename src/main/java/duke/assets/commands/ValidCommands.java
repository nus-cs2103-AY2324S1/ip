package duke.assets.commands;

/**
 * Enum containing all valid user input commands
 */
public enum ValidCommands {
    bye,
    list,
    mark,
    unmark,
    deadline,
    event,
    todo,
    delete,
    find
}
