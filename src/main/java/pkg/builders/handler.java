package pkg.builders;

public class handler {
   // parser (string) -> (arguements)
   // transformer (arguements) -> (data)
   // worker (data) -> (crud + instructions)

   // handle(string) -> (side effects)

   /*  
   func （h *handler) handle (input string) {
    if ok, arguements := parser.Parse(input)； ok {
        worker.Work(transformer.Transform(arguements))
    }
   } 
   */
}
