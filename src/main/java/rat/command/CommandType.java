package rat.command;

/**
 * This enum encapsulates the different types of commands that Rat can handle.
 */
public enum CommandType {
    MARK, UNMARK, TODO, DEADLINE, EVENT, DELETE, LIST, BYE, HELP
}
